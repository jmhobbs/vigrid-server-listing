{
  "name": "vigrid-servers",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/dev.js",
    "clean": "rm -rf dist/ && mkdir -p dist",
    "build": "cp public/* dist/ && node scripts/build.js"
  },
  "author": "John Hobbs",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.2",
    "esbuild-server": "^0.3.0",
    "nchan": "^1.0.10"
  }
}
